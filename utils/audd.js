// Audio fingerprinting fallback via AudD API
// Captures tab audio, sends fingerprint, returns { artist, title, apple_music_id }
// Used when YouTube metadata and title parsing both fail (e.g. Vietnamese reuploads)
